node_modules
.next
data/*.sqlite*
data/*.db
.env
.env.local
*.log
.DS_Store
.turbo
dist
build
coverage


# TypeScript incremental build cache (regenerated on every build)
tsconfig.tsbuildinfo

.vercel
.env*
inline

# Capacitor / Android native shell (regenerated via `npx cap add android`)
out
android
