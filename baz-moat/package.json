{
  "name": "baz-moat",
  "version": "0.1.0",
  "private": true,
  "description": "BAZ Revenue Attribution Diagnostic + tracking-default moat stack. Isolated build — does not touch existing repos.",
  "type": "module",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "diagnostic": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=20" },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}